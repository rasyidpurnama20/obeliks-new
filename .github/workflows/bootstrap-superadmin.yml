name: Bootstrap superadmin

run-name: Bootstrap primary superadmin

on:
  workflow_dispatch:
    inputs:
      site_url:
        description: URL production Vercel, tanpa path
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: bootstrap-superadmin
  cancel-in-progress: false

jobs:
  bootstrap:
    name: Invite and grant superadmin
    runs-on: ubuntu-latest
    environment: Production
    timeout-minutes: 10
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      SUPERADMIN_EMAIL: rasyid.purnama20@gmail.com
      SITE_URL: ${{ inputs.site_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Check required secrets
        shell: bash
        run: |
          if [ -z "$SUPABASE_URL" ] || [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "Add SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY under GitHub Settings > Environments > Production > Environment secrets."
            exit 1
          fi

      - name: Install locked dependencies
        run: npm ci

      - name: Create the first superadmin safely
        run: node scripts/bootstrap-superadmin.mjs
